To set up the project locally on your machine,

a) Install requirements using requirements.txt file :
    pip install -r requirements.txt
b) CD into app folder
c) run the command : uvicorn main:app --host 0.0.0.0 --port 8000

You are all set! no just query the server using postman or thunder 
The two endpoints are /query and /ingest
